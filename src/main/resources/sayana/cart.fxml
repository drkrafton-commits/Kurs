<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox xmlns="http://javafx.com/javafx/17"
      xmlns:fx="http://javafx.com/fxml/1" fx:controller="sayana.Controllers.CartController"
      style="-fx-background-color: linear-gradient(to bottom, #f9f9f9, #fff8e1);">

    <!-- Шапка -->
    <HBox style="-fx-background-color: white; -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 2);">
        <Button text="← Назад" onAction="#handleBack"
                style="-fx-background-color: #636e72; -fx-text-fill: white;
                      -fx-background-radius: 10; -fx-padding: 8 16;"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Text text="🛒 Корзина" style="-fx-font-size: 24px; -fx-font-weight: bold;"/>
        <Region HBox.hgrow="ALWAYS"/>
    </HBox>

    <!-- Контент -->
    <ScrollPane fitToWidth="true" style="-fx-background: transparent;">
        <VBox spacing="20" style="-fx-padding: 30;">
            <Text text="Ваши товары" style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-fill: #2d3436;"/>

            <VBox fx:id="cartItemsContainer" spacing="15"
                  style="-fx-padding: 0 0 20 0;">
                <!-- Товары будут добавляться динамически -->
            </VBox>

            <!-- Итого -->
            <HBox style="-fx-background-color: white; -fx-background-radius: 10; -fx-padding: 20;">
                <Label fx:id="totalLabel" text="Итого: 0 руб."
                       style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #e17055;"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Button text="Очистить корзину" onAction="#handleClearCart"
                        style="-fx-background-color: #ff7675; -fx-text-fill: white;
                              -fx-font-weight: bold; -fx-background-radius: 8; -fx-padding: 10 20;"/>
                <Button fx:id="checkoutButton" text="Оформить заказ" onAction="#handleCheckout"
                        style="-fx-background-color: #00b894; -fx-text-fill: white;
                              -fx-font-weight: bold; -fx-background-radius: 8; -fx-padding: 10 20; -fx-margin-left: 10;"
                />
            </HBox>
        </VBox>
    </ScrollPane>
</VBox>